[package]
name = "y31"
version = "0.1.0"
description = "Local desktop workspace for generated operational software"
edition = "2021"

[lib]
name = "inference_lab_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
