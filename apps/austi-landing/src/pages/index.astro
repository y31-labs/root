---
import Closing from '../components/Closing.astro';
import Faq from '../components/Faq.astro';
import Hero from '../components/Hero.astro';
import Mechanism from '../components/Mechanism.astro';
import SiteFooter from '../components/SiteFooter.astro';
import SiteHeader from '../components/SiteHeader.astro';
import Trust from '../components/Trust.astro';
import Layout from '../layouts/Layout.astro';
import { faqItems } from '../lib/landing-content';

const title = 'Austi | AI Maintenance Coordination for Property Managers';
const description =
  'Austi explores AI-assisted maintenance coordination for residential property managers, connecting tenant reports, approvals, contractors and PMS updates.';
const pilotUrl = 'mailto:hello@austi.works?subject=Maintenance%20workflow%20conversation';
const siteUrl = 'https://austi.works/';
const socialImageUrl = `${siteUrl}og-austi.png`;
const logoUrl = `${siteUrl}austi-logo.svg`;
const structuredData = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'WebSite',
      '@id': `${siteUrl}#website`,
      name: 'Austi',
      url: siteUrl,
      description,
      inLanguage: 'en',
      publisher: { '@id': `${siteUrl}#organization` },
    },
    {
      '@type': 'WebPage',
      '@id': `${siteUrl}#webpage`,
      url: siteUrl,
      name: title,
      description,
      isPartOf: { '@id': `${siteUrl}#website` },
      mainEntity: { '@id': `${siteUrl}#organization` },
      primaryImageOfPage: { '@id': `${siteUrl}#primaryimage` },
      hasPart: { '@id': `${siteUrl}#faq` },
      inLanguage: 'en',
    },
    {
      '@type': 'Organization',
      '@id': `${siteUrl}#organization`,
      name: 'Austi',
      url: siteUrl,
      description,
      logo: logoUrl,
      image: { '@id': `${siteUrl}#primaryimage` },
      areaServed: [
        { '@type': 'Country', name: 'Lithuania' },
        { '@type': 'Country', name: 'Poland' },
      ],
    },
    {
      '@type': 'ImageObject',
      '@id': `${siteUrl}#primaryimage`,
      url: socialImageUrl,
      contentUrl: socialImageUrl,
      width: 1731,
      height: 909,
      caption: 'Austi — AI-assisted maintenance coordination for residential property managers.',
    },
    {
      '@type': 'FAQPage',
      '@id': `${siteUrl}#faq`,
      url: `${siteUrl}#faq`,
      isPartOf: { '@id': `${siteUrl}#webpage` },
      inLanguage: 'en',
      mainEntity: faqItems.map((item) => ({
        '@type': 'Question',
        name: item.question,
        acceptedAnswer: {
          '@type': 'Answer',
          text: item.answer,
        },
      })),
    },
  ],
};
---

<Layout {title} {description} {structuredData}>
  <SiteHeader {pilotUrl} />
  <main id="main-content">
    <Hero {pilotUrl} />
    <Mechanism />
    <Trust />
    <Faq />
    <Closing {pilotUrl} />
  </main>
  <SiteFooter />
</Layout>
